import React from "react";

const ProfilePage = ({ name, email, token }) => {
  return (
    <div>
      <h5>{name}</h5>
      <h5>{email}</h5>
      <h5>{password}</h5>
    </div>
  );
};

export default ProfilePage;
